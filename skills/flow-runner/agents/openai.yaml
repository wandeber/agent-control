interface:
  display_name: "Flow Runner"
  short_description: "Run, resume, or inspect Agent Control flows"
  default_prompt: "Use $flow-runner for the requested flow operation, preserving its execution or inspection scope."
