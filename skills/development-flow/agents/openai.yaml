interface:
  display_name: "Development Flow"
  short_description: "Clarify and run the Agent Control development flow"
  default_prompt: "Use $development-flow to resolve material user decisions for this software task, then run development-flow-v1 with exact plan approval, incremental evidence, optional UAT, and verified closure through Agent Control."
