interface:
  display_name: "Development Flow"
  short_description: "Clarify and run the Agent Control development flow"
  default_prompt: "Use $development-flow to clarify material user choices through the available question interface, prefer clean pre-production work unless confirmed obligations require compatibility, then run development-flow-v1 with exact plan approval, incremental evidence, optional UAT, and verified closure through Agent Control."
