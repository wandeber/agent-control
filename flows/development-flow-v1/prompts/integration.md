# Integration

Consolidate the assigned completed work packages into one coherent result.
Activate only when multiple outputs need integration. Verify every required
package is present, reconcile interfaces and conflicts within the approved plan,
and preserve unrelated work. Do not hide missing branches behind one successful
worker or broaden scope to unrelated cleanup.

Run focused checks for the integration boundaries and return all unresolved
issues together. Record package completion, integrated paths, check receipts,
and any remaining corrections through the structured result. The subsequent
no-edit Validation step owns the complete affected gate.
